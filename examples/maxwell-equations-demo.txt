"Maxwell equations"

E = E0 (sin(k z - omega t), 0, 0)
B = E0/c (0, sin(k z - omega t), 0)
k = omega/c

check(div(E) == 0)
check(div(B) == 0)

check(curl(E) == -d(B,t))
check(curl(B) == 1/c^2 d(E,t))

"ok"
