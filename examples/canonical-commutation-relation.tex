\input{preamble}

\section*{Canonical commutation relation}

Let $X$ and $P$ be the operators
\begin{equation*}
X=x\qquad P=-i\hbar\frac{\partial}{\partial x}
\end{equation*}

Show that
\begin{equation*}
(XP-PX)\psi(x,t)=i\hbar\psi(x,t)
\end{equation*}

Eigenmath code
{\color{blue}
\begin{verbatim}
X(f) = x f
P(f) = -i hbar d(f,x)
X(P(psi(x,t))) - P(X(psi(x,t)))
\end{verbatim}}

Result

\bigskip
$i\hbar\psi(x,t)$

\bigskip
In three dimensions (symbol $\otimes$ is outer product, $\nabla$ is gradient)
\begin{equation*}
X=\begin{pmatrix}x\\y\\z\end{pmatrix}\otimes\qquad P=-i\hbar\nabla
\end{equation*}

Eigenmath code
{\color{blue}
\begin{verbatim}
X(f) = outer((x,y,z),f)
P(f) = -i hbar d(f,(x,y,z))
X(P(psi(x,y,z,t))) - P(X(psi(x,y,z,t)))
\end{verbatim}}

Result

\begin{equation*}
\begin{pmatrix}
i\hbar\psi(x,y,z,t)&0&0\\
0&i\hbar\psi(x,y,z,t)&0\\
0&0&i\hbar\psi(x,y,z,t)
\end{pmatrix}
\end{equation*}

\bigskip
\href{https://georgeweigt.github.io/examples/canonical-commutation-relation-demo.html}{Eigenmath script}

\end{document}
